import {Meta, Canvas} from '@storybook/addon-docs/blocks'
import DocsTable from '../DocTable'
import TypographyExample from './TypographyExample'

import tailwindConfig from '../../tailwind.config.js'

export const TypographyTableRow = ({font, fontSize}) => {
  return (
    <tr>
      <td>
        <TypographyExample className={`font-${font} text-${fontSize}`}>
          Example
        </TypographyExample>
      </td>
      <td>
        <code>{`font-${font} text-${fontSize}`}</code>
      </td>
    </tr>
  )
}

<Meta title="Docs/Typography" />

# Typography

Below is a list of all available fonts along with the utility classes required to style components.

## Primary

<DocsTable>
  <>
    <thead>
      <tr>
        <th>Example</th>
        <th>Class</th>
      </tr>
    </thead>
    <tbody>
      {Object.entries(tailwindConfig.theme.fontSize).map((fontSize, i) => {
        return (
          <TypographyTableRow key={i} font="primary" fontSize={fontSize[0]} />
        )
      })}
    </tbody>
  </>
</DocsTable>

## Secondary

<DocsTable>
  <>
    <thead>
      <tr>
        <th>Example</th>
        <th>Class</th>
      </tr>
    </thead>
    <tbody>
      {Object.entries(tailwindConfig.theme.fontSize).map((fontSize, i) => {
        return (
          <TypographyTableRow key={i} font="secondary" fontSize={fontSize[0]} />
        )
      })}
    </tbody>
  </>
</DocsTable>
